generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model fomapan400hc110 {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model fomapan400ilfosol3 {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model fomapan400ilfoteclc29 {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model fomapan400rodinal {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model fomapan400tmaxdev {
  id        Int     @id @default(autoincrement())
  film      String? @db.VarChar(50)
  developer String? @db.VarChar(50)
  dilution  String? @db.VarChar(10)
  asa_iso   Int?
  time_35mm String? @db.VarChar(10)
  temp      String? @db.VarChar(10)
}

model hp5hc110 {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model hp5ilfosol3 {
  id        Int      @id(map: "hp5ilfosol3_data_pkey") @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model hp5ilfoteclc29 {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model hp5rodinal {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model hp5tmaxdev {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model tmax400hc110 {
  id        Int     @id @default(autoincrement())
  film      String? @db.VarChar(50)
  developer String? @db.VarChar(50)
  dilution  String? @db.VarChar(10)
  asa_iso   Int?
  time_35mm String? @db.VarChar(10)
  temp      String? @db.VarChar(10)
}

model tmax400ilfosol3 {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model tmax400rodinal {
  id        Int      @id @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model tmax400tmaxdev {
  id        Int      @id(map: "tmaxtmaxdev_pkey") @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model User {
  id                                                   Int               @id @default(autoincrement())
  firstName                                            String            @db.VarChar(100)
  lastName                                             String            @db.VarChar(100)
  email                                                String            @unique @db.VarChar(255)
  dateOfBirth                                          DateTime          @db.Date
  password                                             String            @db.VarChar(255)
  createdAt                                            DateTime?         @default(now()) @db.Timestamp(6)
  role                                                 Role?             @default(USER)
  username                                             String            @unique @db.VarChar(100)
  bio                                                  String?
  profileImage                                         String?
  question1_id                                         Int?
  answer1                                              String?
  question2_id                                         Int?
  answer2                                              String?
  comments                                             Comment[]
  likes                                                Like[]
  photos                                               Photo[]
  secret_questions_User_question1_idTosecret_questions secret_questions? @relation("User_question1_idTosecret_questions", fields: [question1_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  secret_questions_User_question2_idTosecret_questions secret_questions? @relation("User_question2_idTosecret_questions", fields: [question2_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model Photo {
  id            Int       @id @default(autoincrement())
  url           String
  userId        Int
  createdAt     DateTime  @default(now())
  title         String?
  likes         Int       @default(0)
  commentsCount Int       @default(0) @map("comments")
  comments      Comment[]
  likedBy       Like[]
  user          User      @relation(fields: [userId], references: [id])
}

model Comment {
  id        Int      @id @default(autoincrement())
  content   String
  userId    Int
  photoId   Int
  createdAt DateTime @default(now())
  photo     Photo    @relation(fields: [photoId], references: [id])
  user      User     @relation(fields: [userId], references: [id])
}

model Like {
  id        Int      @id @default(autoincrement())
  userId    Int
  photoId   Int
  createdAt DateTime @default(now())
  photo     Photo    @relation(fields: [photoId], references: [id])
  user      User     @relation(fields: [userId], references: [id])

  @@unique([userId, photoId])
}

model rpx400hc110 {
  id          Int     @id @default(autoincrement())
  film        String  @db.VarChar(100)
  developer   String  @db.VarChar(50)
  dilution    String  @db.VarChar(10)
  asa_iso     Int
  time_35mm   Decimal @db.Decimal(5, 2)
  temperature String  @db.VarChar(10)
}

model rpx400ilfosol3 {
  id          Int     @id @default(autoincrement())
  film        String  @db.VarChar(100)
  developer   String  @db.VarChar(50)
  dilution    String  @db.VarChar(10)
  asa_iso     Int
  time_35mm   Decimal @db.Decimal(5, 2)
  temperature String  @db.VarChar(10)
}

model rpx400ilfoteclc29 {
  id          Int     @id @default(autoincrement())
  film        String  @db.VarChar(100)
  developer   String  @db.VarChar(50)
  dilution    String  @db.VarChar(10)
  asa_iso     Int
  time_35mm   Decimal @db.Decimal(5, 2)
  temperature String  @db.VarChar(10)
}

model rpx400tmaxdev {
  id          Int     @id @default(autoincrement())
  film        String  @db.VarChar(100)
  developer   String  @db.VarChar(50)
  dilution    String  @db.VarChar(10)
  asa_iso     Int
  time_35mm   Decimal @db.Decimal(5, 2)
  temperature String  @db.VarChar(10)
}

model tmax400ilfoteclc29 {
  id        Int      @id(map: "tmaxilfoteclc29_pkey") @default(autoincrement())
  film      String?  @db.VarChar(50)
  developer String?  @db.VarChar(50)
  dilution  String?  @db.VarChar(10)
  asa_iso   Int?
  time_35mm Decimal? @db.Decimal(5, 2)
  temp      String?  @db.VarChar(10)
}

model trix400hc110 {
  id          Int    @id @default(autoincrement())
  film        String @db.VarChar(100)
  developer   String @db.VarChar(50)
  dilution    String @db.VarChar(10)
  asa_iso     Int
  time_35mm   String @db.VarChar(20)
  temperature String @db.VarChar(10)
}

model trix400ilfosol3 {
  id          Int     @id @default(autoincrement())
  film        String  @db.VarChar(100)
  developer   String  @db.VarChar(50)
  dilution    String  @db.VarChar(10)
  asa_iso     Int
  time_35mm   Decimal @db.Decimal(5, 2)
  temperature String  @db.VarChar(10)
}

model trix400ilfoteclc29 {
  id          Int    @id @default(autoincrement())
  film        String @db.VarChar(100)
  developer   String @db.VarChar(50)
  dilution    String @db.VarChar(10)
  asa_iso     Int
  time_35mm   String @db.VarChar(20)
  temperature String @db.VarChar(10)
}

model trix400rodinal {
  id          Int     @id @default(autoincrement())
  film        String  @db.VarChar(100)
  developer   String  @db.VarChar(50)
  dilution    String  @db.VarChar(10)
  asa_iso     Int
  time_35mm   Decimal @db.Decimal(5, 2)
  temperature String  @db.VarChar(10)
}

model trix400tmaxdev {
  id          Int    @id @default(autoincrement())
  film        String @db.VarChar(100)
  developer   String @db.VarChar(50)
  dilution    String @db.VarChar(10)
  asa_iso     Int
  time_35mm   String @db.VarChar(20)
  temperature String @db.VarChar(10)
}

model secret_questions {
  id                                       Int    @id @default(autoincrement())
  question                                 String
  User_User_question1_idTosecret_questions User[] @relation("User_question1_idTosecret_questions")
  User_User_question2_idTosecret_questions User[] @relation("User_question2_idTosecret_questions")
}

model login_attempts {
  id          Int       @id @default(autoincrement())
  email       String
  ip_address  String    @db.Inet
  user_agent  String?
  success     Boolean   @default(false)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  block_until DateTime? @db.Timestamptz(6)
}

enum Role {
  USER
  ADMIN
}
